<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Post</title>
    
    <style>
        .hero-pattern {
            background-image: 
                radial-gradient(circle at 25% 25%, rgba(59, 130, 246, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 75% 75%, rgba(147, 51, 234, 0.1) 0%, transparent 50%);
        }
        
        .upload-card {
            background: rgba(17, 24, 39, 0.95);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(75, 85, 99, 0.3);
            animation: fadeInUp 0.6s ease-out;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
        }
        
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .upload-button {
            background: linear-gradient(135deg, #3b82f6, #9333ea);
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(59, 130, 246, 0.2);
        }
        
        .upload-button:hover {
            background: linear-gradient(135deg, #2563eb, #7c3aed);
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(59, 130, 246, 0.4);
        }
        
        .back-button {
            background: rgba(75, 85, 99, 0.4);
            border: 1px solid rgba(156, 163, 175, 0.3);
            transition: all 0.3s ease;
        }
        
        .back-button:hover {
            background: rgba(75, 85, 99, 0.6);
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(75, 85, 99, 0.3);
        }
        
        .select-pic-btn {
            color: #3b82f6;
            transition: all 0.3s ease;
            text-decoration: none;
            position: relative;
        }
        
        .select-pic-btn:hover {
            color: #2563eb;
            transform: translateY(-1px);
        }
        
        .select-pic-btn::after {
            content: '';
            position: absolute;
            width: 0;
            height: 2px;
            bottom: -4px;
            left: 50%;
            background: linear-gradient(90deg, #3b82f6, #9333ea);
            transition: all 0.3s ease;
            transform: translateX(-50%);
        }
        
        .select-pic-btn:hover::after {
            width: 100%;
        }
        
        .image-preview {
            width: 120px;
            height: 120px;
            border-radius: 16px;
            border: 2px solid rgba(59, 130, 246, 0.4);
            object-fit: cover;
            transition: all 0.3s ease;
        }
        
        .image-preview:hover {
            transform: scale(1.05);
            box-shadow: 0 8px 25px rgba(59, 130, 246, 0.3);
        }
        
        .image-placeholder {
            width: 120px;
            height: 120px;
            border: 2px dashed rgba(75, 85, 99, 0.5);
            border-radius: 16px;
            background: rgba(31, 41, 55, 0.3);
            transition: all 0.3s ease;
        }
        
        .image-placeholder:hover {
            border-color: rgba(59, 130, 246, 0.5);
            background: rgba(59, 130, 246, 0.1);
        }
        
        .input-field {
            background: rgba(31, 41, 55, 0.8);
            border: 1px solid rgba(75, 85, 99, 0.5);
            backdrop-filter: blur(10px);
            transition: all 0.3s ease;
        }
        
        .input-field:focus {
            border-color: #3b82f6;
            box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
            background: rgba(31, 41, 55, 0.9);
        }
        
        .glass-effect {
            background: linear-gradient(135deg, 
                rgba(255, 255, 255, 0.1) 0%, 
                rgba(255, 255, 255, 0.05) 100%);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .floating-animation {
            animation: float 6s ease-in-out infinite;
        }
        
        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
        }
        
        /* Responsive adjustments */
        @media (max-width: 640px) {
            .image-preview, .image-placeholder {
                width: 100px;
                height: 100px;
            }
            
            .upload-card {
                margin: 1rem;
                padding: 1.5rem;
            }
        }
    </style>
</head>
<body class="min-h-screen bg-gray-900 hero-pattern overflow-x-hidden">
    <div class="relative z-10 w-full max-w-md mx-auto px-4 py-6 min-h-screen flex items-center">
        <div class="upload-card rounded-3xl p-6 w-full glass-effect">
            <h1 class="text-2xl font-bold text-white mb-6 text-center bg-gradient-to-r from-blue-400 to-purple-400 bg-clip-text text-transparent">
                Upload New Post
            </h1>
            
            <!-- Image Selection -->
            <div class="flex flex-col items-center gap-3 mb-6">
                <div id="imageContainer" class="image-placeholder flex items-center justify-center floating-animation">
                    <svg class="w-8 h-8 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                    </svg>
                </div>
                <button type="button" id="selectpic" class="select-pic-btn font-medium text-sm">
                    Select Picture
                </button>
            </div>
            
            <form id="uploadForm" action="/post" method="post" enctype="multipart/form-data" class="space-y-4">
                <input id="imageinp" hidden type="file" name="image" accept="image/*" required>
                
                <!-- Caption -->
                <div>
                    <label for="caption" class="block text-sm font-medium text-gray-300 mb-2">Caption</label>
                    <textarea 
                        id="caption" 
                        name="caption" 
                        rows="3" 
                        class="input-field w-full rounded-xl px-4 py-3 text-white placeholder-gray-400 focus:outline-none resize-none text-sm"
                        placeholder="Write a caption for your post..."
                        required
                    ></textarea>
                </div>
                
                <!-- Buttons -->
                <div class="flex gap-3 pt-2">
                    <button 
                        type="button" 
                        id="backButton"
                        class="back-button flex-1 py-3 px-4 rounded-xl text-white font-medium focus:outline-none text-sm"
                    >
                        Back
                    </button>
                    
                    <button 
                        type="submit" 
                        class="upload-button flex-1 py-3 px-4 rounded-xl text-white font-medium focus:outline-none text-sm"
                    >
                        Upload Post
                    </button>
                </div>
            </form>
        </div>
    </div>
    
    <script>
        // Simple file selection (like their approach)
        document.querySelector("#selectpic").addEventListener("click", function(){
            document.querySelector("#imageinp").click();
        });
        
        // Show image preview when file is selected
        document.querySelector("#imageinp").addEventListener("change", function(e) {
            if (e.target.files[0]) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    document.querySelector("#imageContainer").innerHTML = 
                        `<img src="${e.target.result}" class="image-preview" alt="Preview">`;
                }
                reader.readAsDataURL(e.target.files[0]);
            }
        });
        
        // Back button
        document.getElementById('backButton').addEventListener('click', () => {
            window.history.back();
        });
    </script>
</body>
</html>